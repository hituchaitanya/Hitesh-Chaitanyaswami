# Cheque-OCR: Reference Implementation for Multi-Stage Framework

This reference implementation reproduces the multi-stage pipeline described in the manuscript (PHLS segmentation, ST-CLAHE, SSMHED, texture inpainting, SGCIP-CNN classifier). The implementation includes a synthetic data generator so reviewers can reproduce experiments without private cheque data.

## Quick start
1. Install requirements:
2. Generate synthetic data:
3. Train classifier on synthetic data:
4. Run demo pipeline:
